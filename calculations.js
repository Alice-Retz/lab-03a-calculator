export function add(addX, addY) {
    return addX + addY;
}
export function subtract(subx, suby) {
    return subx - suby;
}
export function multiply(multix, multiy) {
    
}
export function divide(divix, diviy) {
    return divix / diviy;
}
export function modulo(modulox, moduloy) {
    return modulox % moduloy;
}
export function integer(intx, inty) {
    return Math.floor(intx / inty);
}
export function pythagorean(pytx, pyty) {
    return Math.sqrt(pytx ** 2 + pyty ** 2);
}
export function sqfeet(sqfx, sqfy) {
    return Math.pow(sqfx * sqfy, 2);
}